{
  "name": "mfsp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp package.json dist/package.json && cd dist && npm install --only=production",
    "start": "node dist/app.js"
  },
  "author": "Scott Metoyer",
  "license": "ISC",
  "dependencies": {
    "jsmidgen": "^0.1.5",
    "tonal": "^2.2.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.6"
  }
}
